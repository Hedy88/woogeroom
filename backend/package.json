{
    "name": "backend",
    "version": "1.0.0",
    "main": "app.js",
    "scripts": {
        "start": "node app.js",
        "dev": "cross-env WOOZYLOGS=on && nodemon app.js",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "devDependencies": {
        "cross-env": "^7.0.3",
        "nodemon": "^2.0.15",
        "pino-pretty": "^7.3.0"
    },
    "dependencies": {
        "express": "^4.17.2",
        "pino": "^7.6.0",
        "socket.io": "^4.4.0"
    }
}